# نظام مسابقات رمضان التفاعلي 🌙

نظام متكامل لإدارة وتنفيذ المسابقات الرمضانية باستخدام Node.js, Vue.js 3, و MongoDB.

## المميزات
- واجهة عصرية بتنسيق رمضاني.
- فرز تلقائي عادل للمتسابقين بناءً على العمر.
- نظام نقاط ذكي (10 نقاط للإجابة المباشرة، 5 نقاط بعد الخيارات).
- عداد زمني 3 دقائق لكل سؤال.
- مؤثرات بصرية (Confetti & Animations).

## طريقة التشغيل السريع (باستخدام Docker)

1. تأكد من تثبيت **Docker** و **Docker Compose** على جهازك.
2. افتح المجلد في الطرفية (Terminal).
3. قم بتشغيل الأمر التالي:
   ```bash
   docker-compose up --build
   ```
4. بعد اكتمال التشغيل:
   - الواجهة الأمامية: `http://localhost:3030`
   - الواجهة الخلفية (API): `http://localhost:5050`

## دورة العمل الجديدة 🏗️
1. **إدارة الفرق**: اذهب إلى "إدارة الفرق" لإنشاء الفرق وإضافة أعضائها.
2. **إنشاء المسابقة**: عند إنشاء مسابقة، اختر الفريقين اللذين سيتنافسان.
3. **تحديد المشاركين**: لكل مسابقة، اضغط على "تحديد المشاركين" لاختيار الـ 10 متسابقين الذين سيشاركون في هذه اللعبة تحديداً.
4. **تسجيل المتسابقين**: يمكن للمتسابقين التسجيل في الفرق مباشرة عبر رابط التسجيل.

## بيانات الدخول للإدارة
- رابط الدخول: `http://localhost:3030/login`
- اسم المستخدم: `admin`
- كلمة المرور: `ramadan2026`


## ملاحظات
- النظام يستخدم `dev mode` داخل Docker ليسهل عليك التعديل الفوري.
- يفضل استخدام متصفح Chrome أو Edge لأفضل أداء للمؤثرات.

